# Admin Panel

Standalone Nice Windows suite dashboard. It runs from the Windows notification area, registers itself to start with Windows, and opens the panel from its tray menu or double-click action.

Run it with `start.bat`.
